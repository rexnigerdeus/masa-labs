import type { NextConfig } from 'next';

/**
 * Site vitrine The Everyday Co.
 *
 * Déployé séparément de l'application Vitae (brief §8) : deux domaines, deux
 * cycles de vie. Le site est entièrement statique, il n'a ni base de données
 * ni session.
 */
const config: NextConfig = {};

export default config;
